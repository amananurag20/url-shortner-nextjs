export interface UrlData {
  shortId: string;
  originalUrl: string;
}

export interface UrlStorage {
  [key: string]: string;
}

export interface ApiResponse {
  shortId: string;
  message?: string;
}
export interface ApiResponse {
  shortId: string;
  message?: string;
}
